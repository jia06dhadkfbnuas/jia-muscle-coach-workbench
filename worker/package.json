{
  "name": "jia-muscle-coach-sync",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:apply": "wrangler d1 execute jia-muscle-coach --remote --file=schema.sql"
  },
  "devDependencies": {
    "wrangler": "^4.33.0"
  }
}
